---
-
  become: 'true'
  become_user: root
  hosts: all
  name: "Upgrade all packages to the latest version"
  tasks:
    yum:
    name: '*'
    state: latest
-
    name: "Reboot the machine (Wait for 5 min)"
    reboot:
        reboot_timeout: 300
